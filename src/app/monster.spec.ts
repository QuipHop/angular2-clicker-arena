/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {Monster} from './monster';

describe('Monster', () => {
  it('should create an instance', () => {
    expect(new Monster()).toBeTruthy();
  });
});
